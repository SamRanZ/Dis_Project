Project by bts500 (Samuel Rana) and xfv478 (Maximilian Sørensen)

- Download the requirements: pip install -r requirements.txt

- Run postgres server. Put information about servername/database in app.py

- "Load_Database.sql" file in the folder, you should change '/Users/s/Desktop/Dis/Dis_Project/schools.csv' To which ever adress the school.csv file is on your pc.

- Then Load "Load_Database.sql" in pgadmin4

- In terminal go to the directory "Dis_Project" and run: "python app.py"

The server is now up running
Now access the server on the url: http://127.0.0.1:5000/

You can freely search on the different universities on our webapp, as well as click on the different headers to sort them from least to highest and vice versa.